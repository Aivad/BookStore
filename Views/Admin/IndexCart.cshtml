@model List<BookStore.Models.Cart>
@{
    ViewData["Title"] = "Manage Carts";
    Layout = "_AdminLayout";
    var currentPage = (int)ViewBag.Page;
    var totalPages = (int)ViewBag.TotalPages;
    var searchQuery = ViewBag.SearchQuery?.ToString() ?? "";
}

<h2>Manage Carts</h2>

<!--Search Form -->
<form asp-action="IndexCart" method="get" class="d-flex mb-3">
    <input type="text" name="query" class="form-control me-2"
           placeholder="User, Book title, atau Author..."
           value="@searchQuery" />
    <button type="submit" class="btn btn-outline-primary">Search</button>
    @if (!string.IsNullOrEmpty(searchQuery))
    {
        <a asp-action="IndexCart" class="btn btn-outline-secondary ms-2">Clear</a>
    }
</form>

<!--Delete Form -->
<form asp-action="DeleteCart" method="post" id="deleteForm">
    <div class="d-flex justify-content-between mb-3">
        <a asp-action="CreateCart" class="btn btn-success">
            <i class="bi bi-plus"></i> Create New Cart Item
        </a>
        <button type="submit" class="btn btn-danger"
                onclick="return confirm('Are you sure you want to delete selected cart items?');">
            Delete Selected
        </button>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead class="table-dark">
                <tr>
                    <th><input type="checkbox" id="selectAll" /></th>
                    <th>User</th>
                    <th>Book</th>
                    <th>Quantity</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="5" class="text-center">
                            @if (!string.IsNullOrEmpty(searchQuery))
                            {
                                <span>Tidak ada cart yang sesuai dengan pencarian "<strong>@searchQuery</strong>"</span>
                            }
                            else
                            {
                                <span>Tidak ada cart terdaftar.</span>
                            }
                        </td>
                    </tr>
                }
                else
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td><input type="checkbox" name="selectedIds" value="@item.Id" /></td>
                            <td>@item.User?.UserName</td>
                            <td>
                                @if (item.Book != null)
                                {
                                    <div><strong>@item.Book.Title</strong></div>
                                    <small class="text-muted">by @item.Book.Author</small>
                                }
                                else
                                {
                                    <em>Buku telah dihapus</em>
                                }
                            </td>
                            <td>@item.Quantity</td>
                            <td>
                                <a asp-action="EditCart" asp-route-id="@item.Id" class="btn btn-warning btn-sm">
                                    <i class="bi bi-pencil"></i> Edit
                                </a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</form>

<!--Pagination -->
<nav>
    <ul class="pagination mb-0">
        @if (currentPage > 1)
        {
            <li class="page-item">
                <a class="page-link"
                   asp-action="IndexCart"
                   asp-route-page="@(currentPage - 1)"
                   asp-route-query="@searchQuery">Previous</a>
            </li>
        }
        @for (int i = 1; i <= totalPages; i++)
        {
            <li class="page-item @(i == currentPage ? "active" : "")">
                <a class="page-link"
                   asp-action="IndexCart"
                   asp-route-page="@i"
                   asp-route-query="@searchQuery">@i</a>
            </li>
        }
        @if (currentPage < totalPages)
        {
            <li class="page-item">
                <a class="page-link"
                   asp-action="IndexCart"
                   asp-route-page="@(currentPage + 1)"
                   asp-route-query="@searchQuery">Next</a>
            </li>
        }
    </ul>
</nav>

@section Scripts {
    <script>
        document.getElementById('selectAll').addEventListener('change', function () {
            const checkboxes = document.querySelectorAll('input[name="selectedIds"]');
            checkboxes.forEach(cb => cb.checked = this.checked);
        });
    </script>
}